import { LockedColumnDirective } from './locked-column.directive';

describe('LockedColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new LockedColumnDirective();
    expect(directive).toBeTruthy();
  });
});
