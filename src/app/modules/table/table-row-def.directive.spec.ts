import { TableRowDefDirective } from './table-row-def.directive';

describe('TableRowDefDirective', () => {
  it('should create an instance', () => {
    const directive = new TableRowDefDirective();
    expect(directive).toBeTruthy();
  });
});
