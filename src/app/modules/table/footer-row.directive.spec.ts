import { FooterRowDirective } from './footer-row.directive';

describe('FooterRowDirective', () => {
  it('should create an instance', () => {
    const directive = new FooterRowDirective();
    expect(directive).toBeTruthy();
  });
});
