import { PopupMenuDirective } from './popup-menu.directive';

describe('PopupMenuDirective', () => {
  it('should create an instance', () => {
    // const directive = new PopupMenuDirective();
    // expect(directive).toBeTruthy();
  });
});
