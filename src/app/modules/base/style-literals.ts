type AlignHorizontal = 'left' | 'center' | 'right';

type BorderPosition = 'north' | 'south' | 'east' | 'west' | '';

type Layout = 'box' | 'grid' | 'border' | 'flow';

export {
    AlignHorizontal,
    BorderPosition,
    Layout
}
